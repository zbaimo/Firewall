====================================================
  Docker Compose 部署文件已创建完成！
====================================================

已创建的文件：

1. docker-compose.simple.yml   - 简化配置（快速测试）
2. docker-compose.deploy.yml   - 生产部署配置（推荐）⭐
3. deploy.bat                  - Windows一键部署脚本
4. deploy.sh                   - Linux/Mac一键部署脚本
5. DOCKER_COMPOSE_GUIDE.md     - 详细使用指南
6. DEPLOY_GUIDE.md             - 完整部署指南

====================================================
  快速启动（选择一种方式）
====================================================

【方式1】使用部署脚本（最简单）⭐

Windows用户：
  双击运行: deploy.bat
  或命令行: .\deploy.bat

Linux/Mac用户：
  chmod +x deploy.sh
  ./deploy.sh

脚本会自动：
  ✓ 检查Docker环境
  ✓ 选择部署模式
  ✓ 拉取镜像
  ✓ 启动服务
  ✓ 显示访问地址

---

【方式2】简化配置（快速测试）

docker-compose -f docker-compose.simple.yml up -d

特点：
  - 最简配置
  - 开箱即用
  - 适合测试

---

【方式3】生产配置（推荐生产环境）⭐

docker-compose -f docker-compose.deploy.yml up -d

特点：
  - 完整配置
  - 健康检查
  - 资源限制
  - 日志管理
  - 适合生产

---

【方式4】高性能配置

docker-compose -f docker-compose.prod.yml up -d

特点：
  - 主机网络
  - 固定版本
  - 最高性能

====================================================
  配置修改（必须）
====================================================

1. 修改Nginx日志路径

编辑选择的 docker-compose 文件，找到 volumes 部分：

Windows示例：
  volumes:
    - C:/nginx/logs:/var/log/nginx:ro

Linux示例：
  volumes:
    - /var/log/nginx:/var/log/nginx:ro

2. 检查config.yaml

确保配置文件存在并正确配置

====================================================
  访问Web界面
====================================================

启动后访问：
  http://localhost:8080

默认登录：
  用户名: admin
  密码: admin

首次登录会强制修改密码！

====================================================
  常用命令
====================================================

查看服务状态：
  docker-compose -f docker-compose.deploy.yml ps

查看日志：
  docker-compose -f docker-compose.deploy.yml logs -f

停止服务：
  docker-compose -f docker-compose.deploy.yml down

重启服务：
  docker-compose -f docker-compose.deploy.yml restart

更新镜像：
  docker-compose -f docker-compose.deploy.yml pull
  docker-compose -f docker-compose.deploy.yml up -d

====================================================
  详细文档
====================================================

- DOCKER_COMPOSE_GUIDE.md  - 配置文件对比和详细说明
- DEPLOY_GUIDE.md          - 完整部署指南
- DOCKER.md                - Docker使用说明

====================================================
  推荐使用
====================================================

新手/测试：
  运行: deploy.bat (Windows) 或 ./deploy.sh (Linux)
  或: docker-compose -f docker-compose.simple.yml up -d

生产部署：
  docker-compose -f docker-compose.deploy.yml up -d

====================================================

现在就开始部署吧！ 🚀

Windows: 双击 deploy.bat
Linux:   ./deploy.sh

====================================================


